const edad = 20;
const mensaje =
  edad >= 18
    ? `Sí presta el servicio militar`
    : `No presta el servicio militar`;
console.log(mensaje);

if (edad >= 18) {
  console.log(`Sí presta el servicio militar`);
} else {
  console.log(`No presta el servicio militar`);
}
